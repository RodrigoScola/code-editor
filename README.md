# editor

Bare-bones TypeScript starter for a JavaScript program.

## Scripts

- `npm run build` compiles `src/index.ts` into `dist/`
- `npm run dev` recompiles on file changes
- `npm start` runs the compiled output

## Setup

1. Run `npm install`
2. Run `npm run build`
3. Run `npm start`
